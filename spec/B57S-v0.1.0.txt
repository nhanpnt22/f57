============================================================
B57S SYSTEM SPECIFICATION (MINDU)
B57 / H57 / ID57 / R57 / I57 / S57 Unified Stack
Version: v0.1.0 FINAL
Status: Informational (RFC-Grade, Production-Ready)
Date: May 2026
============================================================

============================================================
ABSTRACT
============================================================

B57S defines a unified, layered system for binary encoding,
hash representation, random identification, and identifier 
generation based on:

- B57 (encoding layer)
- H57 (hash representation layer)
- ID57 (identifier profile)
- ID57-SHORT (ultra-compact identifier profile)
- R57 (random generation profile)
- I57 (integration interface)
- S57 (secure composition layer)

It standardizes deterministic, ASCII-safe, canonical, and
entropy-consistent transformations from raw input data to
human-readable outputs.

============================================================
STATUS OF THIS MEMO
============================================================

This document is provided for informational purposes.

This memo is intended for production use.

Implementation coverage note:

- S57 is an optional secure profile within the unified stack.
- Language runtimes MAY implement only B57/H57/ID57/R57/I57
   core APIs while remaining conformant to those core profiles.

============================================================
1. INTRODUCTION
============================================================

B57S defines a composable transformation stack:

 input → HASH → truncate → B57 → string
 random → B57 → string
 
Layer separation:

- B57  → encoding primitive
- H57  → full hash representation
- ID57  → truncated identifier profile
- ID57-SHORT → ultra-compact identifiers 
- R57  → random identifier generation
- I57  → consistent integration interface

Goals:

- deterministic behavior
- canonical representation
- entropy preservation (B57, H57)
- controlled entropy reduction (ID57)
- secure random identifier generation (R57)
- safe domain encapsulation (I57)
- secure keyed composition and AEAD (S57)
- minimal and consistent API design

============================================================
2. CONVENTIONS AND TERMINOLOGY
============================================================

The key words "MUST", "MUST NOT", "REQUIRED", "SHALL",
"SHALL NOT", "SHOULD", "SHOULD NOT", and "MAY" are to be
interpreted as described in RFC 2119 and RFC 8174.

============================================================
3. ARCHITECTURE OVERVIEW
============================================================

------------------------------------------------------------
3.1 B57 (Encoding Layer)
------------------------------------------------------------

 bytes ↔ B57 string

Properties:

- bijective
- deterministic
- entropy-preserving
- canonical

------------------------------------------------------------
3.2 H57 (Hash Representation Layer)
------------------------------------------------------------

 input → HASH → bytes → B57 → string

Definition:

 H57(x) = b57_encode(HASH(x))

Properties:

- full entropy preservation
- canonical representation
- no truncation

------------------------------------------------------------
3.3 ID57 (Identifier Profile)
------------------------------------------------------------

 input → HASH → truncate → B57 → string

Definition:

 ID57(x, L) = b57_encode(truncate_bytes(HASH(x), bits(L)))
 ID57-SHORT(x, L) = ID57(x, L) for small L values.

Properties:

- controlled entropy reduction
- fixed-length identifiers
- one-way transformation

------------------------------------------------------------
3.4 R57 (Random Generation Profile)
------------------------------------------------------------

 random_process → entropy_bytes → B57 → string

Definition:

 R57(mode) = b57_encode(generate_128bit_entropy(mode))

Properties:

- high collision resistance (128-bit)
- versatile generation modes (CSPRNG, Timestamps, Hashing)
- 22-character B57 outputs

------------------------------------------------------------
3.5 I57 (Integration Interface)
------------------------------------------------------------

 A unified programmatic boundary encapsulating all layers.
 
 I57_encode(input_bytes) = B57(input_bytes)
 I57_hash(input_bytes, length_enum) = H57(input_bytes)
 I57_id(input_bytes, length_enum) = ID57(input_bytes, length_enum)
 I57_random(random_profile_param) = B57(random_bytes)

Properties:

- unifies B57, H57, R57, ID57 calls
- acts as the primary developer interface

------------------------------------------------------------
3.6 S57 (Secure Composition Layer)
------------------------------------------------------------

S57 profiles secure composition on top of I57 with:

- domain-separated key derivation
- keyed hash and keyed identifier generation
- AES-256-GCM authenticated encryption/decryption

S57 is layered above I57 and MUST NOT redefine B57 canonical
encoding rules.

------------------------------------------------------------
3.7 Relationship
------------------------------------------------------------

- B57 is the foundation governing character mapping
- H57 builds on B57 for full hashing
- ID57 builds on H57 for identifiers
- R57 builds on B57 using 128-bit constraints
- I57 exposes B57, H57, ID57, and R57 capabilities natively
- S57 composes secure-key and AEAD behavior over I57

Constraint:

 ID57 ⊂ H57
 I57 ⊃ (B57, H57, ID57, R57)
 S57 ⊃ I57

============================================================
4. CORE TRANSFORMATIONS
============================================================

4.1 Encoding (B57):

 b57_encode(bytes) → string
 b57_decode(string) → bytes

4.2 Hash Representation (H57):

 h57 = b57_encode(HASH(input_bytes))

4.3 Identifier Generation (ID57):

 hash_bytes      = HASH(input_bytes)
 effective_bytes = truncate_bytes(hash_bytes, bits(L))
 id57            = b57_encode(effective_bytes)

4.4 Random Generation (R57):

 random_bytes    = _generate_entropy(mode, 16)
 r57             = b57_encode(random_bytes)

4.5 Ecosystem Integration (I57):
 
 b57_encode_alias  = i57_encode(input_bytes)
 h57_hash_alias    = i57_hash(input_bytes, length_enum)
 id57_gen_alias    = i57_id(input_bytes, length_enum)
 rand_alias        = i57_random(random_profile_param)

4.6 Secure Composition (S57):

 secure_keys       = s57_derive_keys(server_secret_key, environment_salt)
 secure_hash       = s57_hash(input_bytes, length_enum)
 secure_id         = s57_id(input_bytes, length_enum)
 secure_ciphertext = s57_encrypt(plaintext, aad)
 secure_plaintext  = s57_decrypt(secure_ciphertext, aad)

============================================================
5. HASH FUNCTION MODEL
============================================================

Hash functions:

- MUST be cryptographic
- MUST be deterministic
- MUST produce fixed-length output
- MUST provide uniform distribution

Recommended:

- BLAKE3 (primary default)
- SHA-256
- SHA-512

Constraints:

- MUST NOT use non-cryptographic hashes
- MUST NOT infer hash function from output

============================================================
6. TRUNCATION MODEL
============================================================

Definition:

 truncate_bytes(hash_bytes, bit_length)

Rules:

- MUST operate on raw bytes
- MUST occur BEFORE B57 encoding
- MUST use prefix truncation
- MUST preserve big-endian ordering
- MUST be deterministic

Bit handling:

 byte_length = ceil(bit_length / 8)

- Excess bits in final byte MUST be truncated

Prohibited:

- truncating encoded strings
- suffix truncation
- post-encoding manipulation

============================================================
7. LENGTH MODEL
============================================================

7.1 Canonical Full-Length (H57):

- 256-bit → 44 chars
- 512-bit → 88 chars

7.2 ID57 Default:

- ID57_DEFAULT = ID57_LEN_128
- bits(ID57_LEN_128) = 128
- output length = 22 chars

7.3 Security Thresholds:

- 2 chars → ≥ 8-bit
- 3 chars → ≥ 16-bit
- 6 chars → ≥ 32-bit
- 11 chars → ≥ 64-bit
- 22 chars → ≥ 128-bit (minimum)
- 44 chars → ≥ 256-bit
- 88 chars → ≥ 512-bit

Constraints:

- MUST use predefined length enums
- MUST NOT accept arbitrary lengths
- Security-sensitive use MUST use ≥ 128-bit

============================================================
8. DETERMINISM AND CANONICAL FORM
============================================================

All layers MUST be deterministic:

 same input → same output

Canonical constraints:

- B57 outputs MUST be canonical
- No normalization or transformation allowed
- Exactly one valid representation per input

============================================================
9. INVARIANTS
============================================================

B57:

 b57_decode(b57_encode(x)) == x

H57:

 H57(x) == b57_encode(HASH(x))

ID57:

 ID57(x, L) ==
 b57_encode(truncate_bytes(HASH(x), bits(L)))

Canonical constraint:

 b57_encode(b57_decode(y)) == y
 (only if y is canonical)

============================================================
10. ERROR MODEL
============================================================

Implementations MUST distinguish:

- INVALID_CHAR
- NON_CANONICAL
- INVALID_LENGTH_ENUM
- ENTROPY_EXCEEDED

Requirements:

- MUST fail deterministically
- MUST NOT continue after error
- MUST NOT use ambiguous errors

============================================================
11. PROHIBITED BEHAVIORS
============================================================

Implementations MUST NOT:

- modify B57 alphabet
- use non-cryptographic hashes
- bypass hashing in ID57/H57
- truncate after encoding
- use suffix truncation
- accept arbitrary lengths
- normalize or transform outputs
- re-hash encoded values
- introduce randomness
- produce non-canonical outputs

============================================================
12. SECURITY CONSIDERATIONS
============================================================

B57S provides no cryptographic guarantees beyond the
underlying hash function.

Security depends on:

- hash function strength (BLAKE3 recommended)
- entropy preservation (H57)
- truncation length (ID57)

Notes:

- H57 preserves full entropy
- ID57 reduces entropy intentionally
- Shorter outputs reduce security proportionally

Encoded length MUST NOT be treated as a guarantee.

============================================================
13. IANA CONSIDERATIONS
============================================================

This document has no IANA actions.

============================================================
14. REFERENCES
============================================================

RFC 2119 - Requirement Levels
RFC 8174 - Requirement Terminology Clarification

============================================================
15. USE CASES
============================================================

B57S defines a composable transformation stack:

 B57  → encoding layer
 H57  → full hash representation
 ID57 → compact identifier profile
 R57  → random generation profile
 I57  → unified integration interface
 S57  → secure keyed composition

Each layer serves a distinct purpose and MUST be used
according to its design constraints.

------------------------------------------------------------
15.1 B57 (ENCODING LAYER)
------------------------------------------------------------

B57 provides a bijective, entropy-preserving encoding
for raw binary data.

Typical use cases:

- binary-to-text conversion
- transport-safe encoding (URLs, filenames)
- human-readable representation of binary blobs
- QR encoding of arbitrary data
- copy/paste-safe tokens

Properties:

- preserves exact input data
- canonical representation
- avoids ambiguous characters

------------------------------------------------------------
15.2 H57 (HASH REPRESENTATION)
------------------------------------------------------------

H57 provides canonical representation of full cryptographic
hash outputs.

Typical use cases:

- file integrity verification
- content fingerprinting
- deduplication systems
- content-addressable storage
- Merkle trees and blockchain structures
- audit logs and tamper detection

Properties:

- preserves full hash entropy
- deterministic and canonical
- suitable for verification and comparison

------------------------------------------------------------
15.3 ID57 (IDENTIFIER PROFILE)
------------------------------------------------------------

ID57 provides compact, fixed-length identifiers derived
from cryptographic hashes.

Typical use cases:

- database primary keys (non-sequential)
- API identifiers
- distributed object IDs
- public resource identifiers
- logging and tracing references

Properties:

- deterministic
- canonical
- length-controlled
- human-readable

------------------------------------------------------------
15.4 R57 (RANDOM GENERATION PROFILE)
------------------------------------------------------------

R57 provides strongly random, un-guessable 128-bit identifiers
mapped safely into the B57 namespace.

Typical use cases:

- session tokens
- user authentication boundaries
- UUIDv4 alternatives
- dynamic, short-lived, single-use keys

Properties:

- secure randomness
- 128-bit collision resistance bound (minimum)
- human-readable, safe strings

------------------------------------------------------------
15.5 I57 (INTEGRATION INTERFACE)
------------------------------------------------------------

I57 provides the outer envelope abstraction to decouple application
logic from B57's internal strict primitives formatting.

Typical use cases:

- simplified developer SDK consumption
- strict validation of already-generated H57, ID57 values

Properties:

- simplifies and standardizes the API

------------------------------------------------------------
15.6 S57 (SECURE COMPOSITION LAYER)
------------------------------------------------------------

S57 provides secure composition for keyed and encrypted flows.

Typical use cases:

- authenticated encryption of payloads
- keyed deterministic identifiers
- domain-separated key management

15.7 SYSTEM-LEVEL USE (B57S STACK)
------------------------------------------------------------

B57S enables full pipeline composition:

 input → HASH → truncate → B57 → identifier

Typical system patterns:

1. Dual-layer identity:

   full_hash = H57(data)
   short_id  = ID57(data, ID57_LEN_128)

2. Storage vs exposure:

   store using H57
   expose ID57 externally

3. Deduplication:

   compare H57 for exact match
   index via ID57 for efficiency

4. Novel Random Objects:
   
   obj_key = R57(mode=CSPRNG)

------------------------------------------------------------
15.8 SECURITY-SENSITIVE VS NON-SENSITIVE USE
------------------------------------------------------------

Security-sensitive:

- MUST use ≥ ID57_LEN_128
- SHOULD use H57 when full entropy is required
- MUST use strong hash functions (e.g., BLAKE3)
- MUST use R57 CSPRNG mode if deterministic generation is omitted

Non-sensitive / UX-focused:

- MAY use shorter ID57 lengths
- MUST implement collision handling

------------------------------------------------------------
15.9 NON-GOALS
------------------------------------------------------------

B57, H57, ID57, R57, and I57 core layers MUST NOT be used for:

- encryption
- reversible encoding of sensitive data
- explicit symmetric/asymmetric cryptosystems

S57 defines secure composition and uses cryptographic
primitives, but does not replace independent cryptographic
review and operational controls.

------------------------------------------------------------
15.10 DESIGN SUMMARY
------------------------------------------------------------

Layer selection:

- B57  → exact binary representation
- H57  → full hash integrity
- ID57 → compact identifiers
- R57  → random identifiers
- I57  → outer interface logic
- S57  → secure keyed and encrypted composition

============================================================
16. ID57-SHORT (ULTRA-COMPACT IDENTIFIERS)
============================================================

ID57 supports ultra-compact identifiers starting from
length 4 characters, optimized for human-centric usage.

Primary applications:

- short URLs
- QR codes
- user-visible IDs
- UI references
- temporary or scoped identifiers

Typical lengths:

- 4 chars → ~23-bit
- 5 chars → ~29-bit
- 6 chars → ~32-bit
- 8 chars → ~47-bit

------------------------------------------------------------
16.1 DESIGN INTENT
------------------------------------------------------------

Ultra-compact identifiers prioritize:

- minimal visual size
- fast manual entry
- low QR density
- usability in constrained environments

They are NOT designed for strong uniqueness guarantees.

------------------------------------------------------------
16.2 COLLISION CHARACTERISTICS
------------------------------------------------------------

Short identifiers have limited entropy and higher collision
probability.

Approximate practical limits:

- 4 chars (~23-bit) → small datasets (~thousands)
- 5 chars (~29-bit) → medium datasets (~tens of thousands)
- 8 chars (~47-bit) → larger datasets (~millions)

Values are approximate and depend on acceptable risk.

------------------------------------------------------------
16.3 REQUIRED SAFETY CONSTRAINTS
------------------------------------------------------------

Implementations using ultra-compact IDs MUST:

- perform collision detection
- support regeneration on collision
- scope identifiers within a namespace
- avoid global uniqueness assumptions
- treat identifiers as non-secure

------------------------------------------------------------
16.4 RECOMMENDED PATTERNS
------------------------------------------------------------

Pattern 1: Short + Full Mapping

 short_id = ID57(x, ID57_LEN_23)
 full_id  = H57(x)

- short_id used externally
- full_id used internally

------------------------------------------------------------

Pattern 2: Namespaced IDs

 short_id = ID57(namespace || x, ID57_LEN_23)

- reduces collision domain
- enables contextual uniqueness

------------------------------------------------------------

Pattern 3: Collision-Aware Allocation

 loop:
   id = ID57(x || counter, ID57_LEN_23)
   if not exists(id):
     store(id)
     break

------------------------------------------------------------
16.5 QR CODE USAGE
------------------------------------------------------------

ID57 short identifiers are well-suited for QR encoding:

- fewer characters → smaller QR size
- faster scanning
- better readability

Recommended:

- 4–8 chars → temporary / offline use
- ≥12 chars → persistent identifiers

------------------------------------------------------------
16.6 PROHIBITED USAGE
------------------------------------------------------------

Ultra-compact ID57 MUST NOT be used for:

- authentication
- authorization
- security tokens
- globally unique identifiers
- cryptographic identity

------------------------------------------------------------
16.7 ESCALATION RULE
------------------------------------------------------------

Systems MUST increase length when:

- dataset grows
- collision rate increases
- identifiers become cross-system

Recommended progression:

 4 → 8 → 12 → 16 → 22 chars

------------------------------------------------------------
16.8 DESIGN SUMMARY
------------------------------------------------------------

Ultra-compact ID57 identifiers are:

- human-optimized
- space-efficient
- collision-prone by design

They MUST be treated as:

- convenience identifiers
- scoped identifiers
- not security primitives

============================================================
17. AUTHOR
============================================================

 Pham Nhan

============================================================
END
============================================================