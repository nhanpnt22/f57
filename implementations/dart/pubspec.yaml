name: b57
description: B57 binary-to-text encoding in Dart with H57, ID57, ID57-SHORT, I57, and R57 profiles.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.0.0

dependencies:
  crypto: ^3.0.0
  hex: ^0.2.0
  convert: ^3.0.0

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
